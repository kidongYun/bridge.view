class CellType {
    public cellId: number
    public type: string

    constructor(cellId: number, type: string) {
        this.cellId = cellId
        this.type = type
    }
}

export default CellType;