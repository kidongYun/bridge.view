class TabType {
    type: string
    title: string
    event: string

    constructor(type: string, title: string, event: string) {
        this.type = type;
        this.title = title;
        this.event = event;
    }
}

export default TabType;